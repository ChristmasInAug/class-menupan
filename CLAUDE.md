# CLAUDE.md

카페 메뉴판 (Excel 구동) 프로젝트. Excel 탭 1개 = 메뉴판 페이지 1개, 저장 시 SSE로 화면 즉시 갱신.

- 제품 요구사항(무엇을/왜): [docs/PRD.md](docs/PRD.md)
- 기술 요구사항(어떻게): [docs/TRD.md](docs/TRD.md)
- 원본 요구사항 원문: [docs/menu-project-requirement.md](docs/menu-project-requirement.md)
- **TDD 실행 문서**: [TDD-IMPLEMENTS.md](TDD-IMPLEMENTS.md) — 항목별 상태(⬜/🔴/🟢/✅) 추적. `/tdd-red` → `/tdd-green` → `/tdd-refactor` 슬래시 명령(`.claude/commands/`)으로 항목을 하나씩 진행

이 저장소는 코드 없는 그린필드 상태다. 첫 구현부터 아래 TDD 프로세스를 따른다.

---

## 개발 원칙 — 켄트 벡 증강코딩 (TDD)

출처: [Kentbeck-CLAUDE-ko.md](https://github.com/claude-code-expert/inflearn-docs/blob/main/example/Kentbeck-CLAUDE-ko.md)

핵심 철학: **깨끗하고 잘 테스트된 코드를 빠른 구현보다 항상 우선한다.**

### 1. TDD 사이클: Red → Green → Refactor

- **Red**: 기능의 작은 부분에 대해 의도를 명확히 하는 실패하는 테스트를 먼저 작성한다. 테스트 이름은 행위를 설명하는 방식으로 짓는다(예: `shouldMarkItemSoldOutWhenFlagIsY`). 실패가 명확하고 유용한 정보를 제공해야 한다.
- **Green**: 테스트를 통과시키는 데 필요한 최소한의 코드만 구현한다. 과도한 기능이나 최적화는 이 단계에서 절대 금지. 모든 테스트가 통과하는 상태에 도달한다.
- **Refactor**: 테스트가 통과한 뒤에만 리팩터링한다. 한 번에 하나의 변경만 수행하고, 각 단계마다 테스트를 재실행한다. 중복 제거와 명확성 향상에 집중한다.

### 2. Tidy First: 구조적 변경 vs 행위적 변경

모든 변경을 두 범주로 명확히 구분한다.

- **구조적 변경**: 행위를 바꾸지 않고 코드를 재배열 — 이름 변경, 메서드 추출, 코드 이동, 모듈 재구성. 기능은 정확히 동일하게 유지.
- **행위적 변경**: 실제 기능 추가/수정 — 새 기능 구현, 버그 수정, 로직 변경. 시스템 동작 방식 자체를 변경.

**핵심 규칙**: 같은 커밋에 구조적 변경과 행위적 변경을 절대 섞지 않는다. 항상 구조적 변경을 먼저 수행한다. 구조적 변경 전후로 테스트를 실행해 행위 불변성을 검증한다.

### 3. 커밋 규율

커밋 가능 조건 (모두 충족해야 함):
1. 모든 테스트가 통과
2. 모든 컴파일러/린터 경고 해결
3. 변경이 하나의 논리적 작업 단위를 나타냄
4. 커밋 메시지가 구조적/행위적 변경 여부를 명확히 표시

크고 드문 커밋보다 **작고 잦은 커밋**을 사용한다. 각 커밋은 명확한 의도와 범위를 지녀 추적 가능성과 되돌리기 용이성을 극대화한다.

이 프로젝트에서의 커밋 메시지 prefix 예시:
- `structural: extract parseSheetRow from parseWorkbook` — 구조적 변경
- `feat: mark item sold-out when 품절 column is Y` — 행위적 변경(신규 기능)
- `fix: debounce timer not reset on rapid saves` — 행위적 변경(버그 수정)

### 4. 코드 품질 기준

- **중복 제거**: 동일한 코드/로직 반복을 가차 없이 제거(DRY).
- **명확한 의도 표현**: 이름과 구조로 목적이 명백해야 함.
- **의존성의 명시성**: 외부 의존성을 숨기지 않고 인자/반환값으로 드러냄.
- **메서드 설계**: 작게 유지, 단일 책임 원칙, 상태와 부작용 최소화.
- **단순성 우선**: 동작할 수 있는 가장 단순한 해법을 사용. 과도한 추상화·일반화 회피.

### 5. 버그 수정 프로세스

1. API 수준의 실패하는 테스트를 먼저 작성한다(그 버그를 노출시킬 테스트).
2. 문제를 재현하는 가장 작은 테스트를 작성한다.
3. 두 테스트를 모두 통과시키는 코드를 구현한다.

### 6. 리팩터링 가이드라인

- 테스트가 통과할 때에만 리팩터링한다(Green 단계 이후).
- 정립된 리팩터링 패턴을 그 고유 이름과 함께 사용한다(Extract Method, Rename 등).
- 한 번에 하나의 변경만 수행하고, 각 단계마다 테스트를 실행한다.
- 중복 제거를 최우선, 명확성 향상을 그다음 우선순위로 한다.

### 7. 실제 워크플로우 (새 기능 구현 시)

1. 테스트 작성: 기능의 작은 일부에 대한 단순한 실패 테스트 작성
2. 최소 구현: 통과시키는 최소한의 코드만 작성
3. 테스트 실행: 통과 확인 (Green)
4. 구조적 개선: 필요한 구조적 변경 수행, 각 변경 후 테스트 실행
5. 구조적 커밋: 구조적 변경을 행위적 변경과 분리해 커밋
6. 다음 기능: 또 다른 실패하는 테스트 추가
7. 반복: 기능이 완성될 때까지 반복(각 커밋이 명확히 구분됨)

테스트 실행 규칙: **매번 모든 테스트를 실행한다**(오래 걸리는 테스트는 제외 가능). 각 단계별로 테스트 상태를 확인해 회귀를 방지한다.

**기억할 것**: 한 번에 하나의 테스트를 작성하고, 그것을 동작하게 한 뒤, 구조를 개선한다.

### 이 프로젝트에서의 적용 단위

아직 코드/테스트 러너가 없으므로 구현 착수 시 다음 순서로 Red-Green-Refactor를 적용한다(상세 계약은 [docs/TRD.md](docs/TRD.md) 참조):

1. Excel 파서(시트 → JSON 변환, 품절 Y/빈칸, `_` 접두 시트 제외, 가격 숫자 파싱) 단위 테스트부터 시작
2. `_설정` 시트 파싱(매장명/테마/자동전환초) 단위 테스트
3. `GET /api/menu` 라우트 통합 테스트
4. chokidar 파일 감시 + 300ms 디바운스 동작 테스트
5. SSE `menu-updated` 브로드캐스트 테스트
6. 클라이언트 렌더링(테마 CSS 변수 적용, 품절 표시)은 가능한 범위에서 순수 함수 단위로 분리해 테스트

## 코딩 스타일

이미지·HTML·SVG 생성 시 [.claude/rules/anti-ai-slop.md](.claude/rules/anti-ai-slop.md)를 따른다(그라데이션·글로우·장식 모션 금지, 무채색+액센트 1색, 여백/정렬로 위계 표현).

## 빠른 시작

```bash
npm install
npm start
# → http://localhost:3000
# menu.xlsx 수정 후 저장(Ctrl+S) → 화면 자동 갱신
```

상세 폴더 구조·Excel 작성 규칙·동작 원리는 [docs/TRD.md](docs/TRD.md), 제품 배경·기능 요구사항은 [docs/PRD.md](docs/PRD.md) 참조.



## 프롬프트 기록 훅

`.claude/settings.local.json`에 **Stop 훅**이 걸려 있어, 세션 종료 시 `extract-my-prompts.sh`가 이 폴더의 Claude Code 세션에서 사용자가 입력한 프롬프트만 뽑아 [Prompt.md](Prompt.md)에 append한다. `settings.local.json`은 개인/머신 전용이라 `.gitignore` 처리돼 있다(커밋되지 않음).



## TDD 필수 규칙

### Red-Green-Refactor 사이클
1. Red: 먼저 실패하는 테스트를 작성한다
   - 테스트가 실제로 실패하는지 확인
2. Green: 테스트를 통과시키는 최소한의 코드 작성
   - 하드코딩도 허용 (다음 테스트가 일반화를 강제)
3. Refactor: 테스트가 통과하는 상태에서 코드 개선
   - 중복 제거, 명확한 이름 사용

### 금지 사항
- ❌ 테스트 없이 구현 코드 작성
- ❌ 테스트와 구현을 동시에 작성
- ❌ 테스트 skip 또는 주석 처리
- ❌ 실패 원인 분석 없이 테스트 수정